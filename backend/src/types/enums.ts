export enum UserRole {
  ADMIN = 'ADMIN',
  PRINCIPAL = 'PRINCIPAL',
  OPERATOR = 'OPERATOR'
}

export enum DatabaseStatus {
  CONNECTED = 'connected',
  DISCONNECTED = 'disconnected'
}

export enum ApiStatus {
  OK = 'OK',
  DEGRADED = 'DEGRADED'
}